var AppView = Backbone.View.extend({
    id: 'application',

    events: function() {
        return {
            // 'ready': 'resizePhases'
        }
    },

    initialize: function() {
    },
});
